Just a first initial attempt at some recursion using ruby. Backs out of current folder that you're in and if it encounters any HTML files it will output a prompt notifying that it found an html file.

Had the program initially renaming files but realized that was probably not the best idea :D !
